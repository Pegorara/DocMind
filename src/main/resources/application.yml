spring:
  application:
    name: docmind

  datasource:
    url: jdbc:postgresql://localhost:5433/${POSTGRES_DB:docmind}
    username: ${POSTGRES_USER:docmind}
    password: ${POSTGRES_PASSWORD:docmind}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: public

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.pegorara.docmind: DEBUG
    org.hibernate.SQL: DEBUG

docmind:
  storage:
    local:
      base-path: ./storage/documents
  upload:
    max-size-bytes: 20971520 # 20 MB